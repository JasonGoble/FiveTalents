meta {
  name: Remove Member from Family - Not Found
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/api/families/1/members/999999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
