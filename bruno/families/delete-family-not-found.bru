meta {
  name: Delete Family - Not Found
  type: http
  seq: 24
}

delete {
  url: {{baseUrl}}/api/families/999999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
