meta {
  name: Get Families - No Token
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/families?organizationId={{organizationId}}
  body: none
  auth: none
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
