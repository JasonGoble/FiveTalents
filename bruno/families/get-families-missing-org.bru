meta {
  name: Get Families - Missing organizationId
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/families
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 200 or 400", function() {
    expect([200, 400]).to.include(res.status);
  });
}
