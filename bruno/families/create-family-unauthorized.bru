meta {
  name: Create Family - No Token
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/families
  body: json
  auth: none
}

body:json {
  {
    "name": "Should Not Be Created",
    "organizationId": {{organizationId}}
  }
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
