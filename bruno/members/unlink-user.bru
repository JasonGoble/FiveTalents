meta {
  name: Unlink User from Member (Admin)
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/api/members/{{memberId}}/link-user
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
