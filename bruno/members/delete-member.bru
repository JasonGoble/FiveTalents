meta {
  name: Delete Member
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/api/members/{{memberId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
