meta {
  name: Create Member - Missing Required Fields
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/members
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "organizationId": {{organizationId}}
  }
}

tests {
  test("status 400", function() {
    expect(res.status).to.equal(400);
  });
}
