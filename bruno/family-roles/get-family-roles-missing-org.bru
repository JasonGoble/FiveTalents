meta {
  name: Get Family Roles - Missing organizationId
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/family-roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 200 or 400", function() {
    expect([200, 400]).to.include(res.status);
  });
}
