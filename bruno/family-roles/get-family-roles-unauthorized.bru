meta {
  name: Get Family Roles - No Token
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/family-roles?organizationId={{organizationId}}
  body: none
  auth: none
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
